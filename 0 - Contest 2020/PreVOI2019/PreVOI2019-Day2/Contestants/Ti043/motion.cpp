#include <bits/stdc++.h>
#define FOR(i, a, b) for(int i = (a); i <= (b); i++)
#define FORR(i, a, b) for(int i = (a); i >= (b); i--)
#define maxn

using namespace std;
int n;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    ifstream cin("motion.inp");
    ofstream cout("motion.out");
    cin >> n;
    while(n--){
        cout << -1<<'\n';
    }
}
