#include <bits/stdc++.h>
#define int long long

using namespace std;

int32_t main()
{
    if(fopen("motion.inp", "r")) freopen("motion.inp", "r", stdin), freopen("motion.out", "w", stdout);
    int n;
    cin >> n;
    for(int i = 1; i <= n; i++) cout << -1 << '\n';
}
